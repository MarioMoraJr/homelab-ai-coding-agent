{
  "patterns": [
    {
      "id": "express-query-in-route",
      "match": "PathError.*Unexpected \\?",
      "hint": "Express route paths cannot contain '?'. Register the route without the query string (e.g. app.get('/search', ...)) and read the parameter inside the handler with req.query.paramName."
    },
    {
      "id": "undefined-property",
      "match": "Cannot read propert(?:y|ies) of undefined",
      "hint": "A value is undefined when you try to access a property on it. Add a guard before use, e.g.: if (!req.query.q) return res.status(400).json({ error: 'q is required' });"
    },
    {
      "id": "duplicate-identifier",
      "match": "Identifier '(\\w+)' has already been declared",
      "hint": "Variable '$1' was declared twice with const in the same scope. Every const must have a unique name."
    },
    {
      "id": "assert-empty-array",
      "match": "\\+ \\[\\]\\n[- ]+\\[",
      "hint": "Assertion got an empty array but expected data. Most likely cause: the data was deleted or never created before this line. Check whether a DELETE or clear-all step runs before this assertion and move the assertion earlier if needed."
    },
    {
      "id": "assert-deep-equal",
      "match": "Expected values to be strictly deep-equal",
      "hint": "Deep-equal assertion failed. Verify that your expected object includes ALL fields the server returns, including null fields (e.g. author: null). Read the server handler to confirm the exact response shape."
    },
    {
      "id": "module-not-found-relative",
      "match": "Cannot find module '((?:\\.\\.?/)[^']+)'[\\s\\S]*?Require stack:[\\s\\S]*?- ([^\\n]+)",
      "hint": "The require path '$1' does not exist. Check the file shown in the Require stack ('$2') — it has a bad require() call. Only require modules that actually exist in the project. Use require('./server') to import the Express app (no .js extension). Do NOT invent new files like './data/quotes'."
    },
    {
      "id": "module-not-found-npm",
      "match": "Cannot find module '([^./'][^']*)'",
      "hint": "npm package '$1' is not installed. Run: npm install $1"
    },
    {
      "id": "app-listen-not-function",
      "match": "app\\.listen is not a function",
      "hint": "server.js is not exporting the Express app correctly. The file must end with `module.exports = app;` — not `module.exports = server` or the result of app.listen(). Also start the server only conditionally: `if (require.main === module) { app.listen(port, cb); }` so tests can import app without starting a server."
    },
    {
      "id": "server-started-at-module-level",
      "match": "Server(?:is)? running on port \\d+",
      "hint": "server.js started a listening server when it was require()d by the test. The server must only start conditionally: `if (require.main === module) { app.listen(port, cb); }` and must export `module.exports = app;`. Tests start their own server on port 0."
    },
    {
      "id": "command-timed-out",
      "match": "ERROR: command timed out",
      "hint": "The test command hung and was killed. Most likely cause: the test file called app.listen() or a server.listen() at the top level outside any test() block, keeping the process alive forever. Move all server startup inside a test() block: const server = await new Promise(resolve => { const s = app.listen(0, () => resolve(s)); }); and always call t.after(() => server.close()) to clean up."
    },
    {
      "id": "econnrefused-hardcoded-port",
      "match": "connect ECONNREFUSED 127\\.0\\.0\\.1:(\\d+)",
      "hint": "The test is trying to connect to port $1 but nothing is listening there. Tests must start the server themselves on port 0 and use the assigned port: const server = await new Promise(resolve => { const s = app.listen(0, () => resolve(s)); }); const base = `http://127.0.0.1:${server.address().port}`; Do not hardcode any port number. Do not call API helpers outside a test() block."
    },
    {
      "id": "port-in-use",
      "match": "EADDRINUSE",
      "hint": "Port already in use. In tests, pass port 0 to listen so the OS picks a free port: app.listen(0, cb) and read the actual port from server.address().port."
    },
    {
      "id": "syntax-error-in-named-file",
      "match": "/workspace/[^/]+/([^:\\n]+):(\\d+)[\\s\\S]{0,300}SyntaxError: ([^\\n]+)",
      "hint": "The SyntaxError '$3' is in '$1' at line $2 — NOT in server.test.js. Read '$1' immediately, find the broken syntax near line $2, then use write_file to rewrite '$1' completely with correct syntax. Do NOT touch server.test.js until '$1' parses without errors."
    },
    {
      "id": "syntax-error",
      "match": "SyntaxError: (.*)",
      "hint": "Syntax error: $1. Read the file the error points to (check which .js file appears above 'SyntaxError' in the output), then use write_file to rewrite that file completely with correct syntax."
    },
    {
      "id": "wrong-test-framework",
      "match": "ReferenceError: (describe|it|beforeAll|afterAll|beforeEach|afterEach) is not defined",
      "hint": "This project uses node:test, not Jest/Mocha. Remove '$1' and all other Jest globals. The test file must use: const test = require('node:test'); const assert = require('node:assert/strict'); — then wrap everything in a single test('name', async (t) => { ... }) block. The test file must be at server.test.js in the project root, NOT inside a tests/ subdirectory. Delete any tests/ directory you created."
    },
    {
      "id": "server-not-defined-in-listen",
      "match": "ReferenceError: server is not defined[\\s\\S]*?server\\.js",
      "hint": "server.js references 'server' before it is assigned. Do not use a variable called 'server' inside server.js — that name conflicts with the server object in the test. Export only the express app: module.exports = app; and start it conditionally with: if (require.main === module) { app.listen(port, cb); }"
    },
    {
      "id": "require-in-esm",
      "match": "require is not defined in ES module scope",
      "hint": "server.test.js was written with top-level await, which makes Node.js treat it as an ES module — but require() only works in CommonJS. Fix: use write_file to completely rewrite server.test.js using only CommonJS style. Start with: const test = require('node:test'); const assert = require('node:assert/strict'); const app = require('./server'); — then wrap ALL code inside a single test('quote API', async (t) => { ... }) block. NEVER use top-level await or top-level async code outside that block."
    }
  ]
}
